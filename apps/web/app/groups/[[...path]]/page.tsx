export const metadata = { title: 'Group Chat' }

// The persistent parent layout owns the iframe. This optional catch-all only
// makes every mirrored Armada URL a valid Next route.
export default function GroupsPage(): null {
  return null
}
